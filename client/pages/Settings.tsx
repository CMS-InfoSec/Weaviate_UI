import Layout from "@/components/Layout";
import PlaceholderPage from "@/components/PlaceholderPage";

export default function Settings() {
  return (
    <Layout>
      <PlaceholderPage
        title="Settings"
        description="Configure general settings, API keys, and authentication"
      />
    </Layout>
  );
}
